import { type Metadata } from 'next'

export const metadata: Metadata = {
  description: 'Dashboard Page',
  title: 'Dashboard',
}

const Dashboard = () => <h1>{'Hello, Dashboard Page!'}</h1>

export default Dashboard

